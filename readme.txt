Add:

body {
  overflow-x: hidden;
}

to global.scss at the end of every project to cater for devices that have variable screen navigation devices.

git init
git add .
git commit -m "initial commit"
git status
git remote add origin https://github.com/charlie-george-1989/good-things.git
git remote -v
git push origin master
git branch
git status
git push origin master
git status
